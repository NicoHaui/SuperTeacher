sudo: required
dist: trusty
language: cpp
compiler:
- gcc
- clang
addons:
  apt:
    packages:
    - cmake
    - libsdl2-dev
    - asciidoc
script:
    - "mkdir build && cd build && cmake ../ && make && make install && cd .."
    - "cd docs && ./build-travis.sh && cd .."
